# AGENTS

## プロジェクト概要
- このワークスペースは電文(バイナリ)構成をとるログの生成と解析を題材とした
  練習用のプロジェクトである。
- 電文を生成するプログラムと生成された電文を解析するプログラムの両方を実装する。


## ディレクトリ構成

- README.md : プロジェクトについての情報を記載している。
- spec : 電文(バイナリ)構成に関する仕様
  - 現状未配置である。
- c/ : C言語による実装である。

今後、比較のため C++、Haskell、Rust、Elixirなどによる実装を予定している。

## 実装方針 C言語

- 厳密なエラー処理は不要である。
- 性能よりも以下を優先する。
  - 読みやすさ
  - 保守性
  - 拡張性
- プログラムのビルドにはMakefileを利用する。
- 不要な依存関係は避ける。

## 実装方針 C++

- 既存のCの出力を正とする。
- C++は学習用途である。設計的な提案を歓迎する。
  - オブジェクト指向的な実装案
  - 関数型プログラミング的な実装案
  - これらのミックス
- 性能用よりも以下を優先する。
  - 読みやすさ
  - 保守性
  - 拡張性
- 最新のg++の機能を取り入れる。
- プログラムのビルドにはMakefileを利用する。
  - プログラムの変更時にはビルド確認を行う。
- 不要な依存関係は避ける。

## コード編集について

- 説明箇所は基本的に日本語とする。
- コメントは基本的に日本語とする。
- 括弧は半角を利用する。
